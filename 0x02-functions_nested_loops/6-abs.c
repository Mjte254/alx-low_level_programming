#include "main.h"

/**
<<<<<<< HEAD
 * _abs - Computes the absolute value of an integer
 * @t: The integer to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
=======
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
>>>>>>> b8ecbf4 (6. There is no such thing as absolute value in this world. You can only estimate what a thing is worth to you)
}
